{
  "mcpServers": {
    "media": {
      "command": "python",
      "args": ["${CLAUDE_PLUGIN_ROOT}/servers/media-server/server.py"],
      "env": {
        "PYTHONPATH": "${CLAUDE_PLUGIN_ROOT}/scripts"
      }
    }
  }
}
